# OpenMM/PDBFixer are best installed via conda-forge.
# If you must use pip, install openmm wheels first, then:
mdtraj>=1.9
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8
pyyaml>=6.0
jupyterlab
